extends base
 
block content
    h4 Change of Title of Video 
    form(method="post")
        input(name="title" placeholder="Video Title", value=video.title, required)
        input(type="submit", value="Save")


